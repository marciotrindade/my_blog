== README

* Ruby version
  ruby-2.0.0-p353

* System dependencies
  nodejs, libxslt-dev, libxml2-dev, nginx

* Configuration
  cp config/database.yml.example config/database.yml

* Database creation
  rake db:create

* Database initialization
  rake db:migrate

* How to run the test suite
  rspec spec

* Services (job queues, cache servers, search engines, etc.)
  memcached

* Deployment instructions
  cap deploy
