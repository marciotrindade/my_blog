<!DOCTYPE html>
<html class="ls-theme-green">
<head>
  <title>Admin - Marcio Trindade</title>
  <meta charset="utf-8">
  <meta content="IE=edge,chrome=1" http-equiv="X-UA-Compatible">
  <meta name="viewport" content="width=device-width, initial-scale=1, user-scalable=no">
  <link href="<%= AppConfig.locastyle %>/stylesheets/locastyle.css" rel="stylesheet" type="text/css">
  <%= csrf_meta_tags %>
</head>

<body>
  <!-- Header -->
  <div class="ls-topbar">
    <span class="ls-show-sidebar ls-ico-menu"></span>

    <div class="ls-notification-topbar">
    <!-- User info -->
      <div data-ls-module="dropdown" class="ls-dropdown ls-user-account">
        <a href="#" class="ls-ico-user"><%= current_user.name %></a>
        <nav class="ls-dropdown-nav ls-user-menu">
          <ul>
            <li><%= link_to t(:logout), destroy_user_session_path, class: "help-suggestions hidden-xs", method: :delete %></li>
          </ul>
        </nav>
      </div>
    </div>
    <!-- Nome do produto/marca -->
    <h1 class="ls-brand-name"><%= link_to AppConfig.site.name, admin_root_path, title: AppConfig.site.name %></h1>
  </div>

  <main class="ls-main ">
    <!-- Content -->
    <div class="container-fluid">
      <h1 class="ls-title-intro <%= title_ico %>"><%= content_title %></h1>
      <%= flash_message %>
      <%= yield %>
    </div>

    <!-- Footer -->
    <footer class="ls-footer" role="contentinfo">
      <nav class="ls-footer-menu">
        <h2 class="ls-title-footer">Support</h2>
      </nav>
      <div class="ls-footer-info">
        <p class="ls-copy-right">Copyright © <%= Date.today.year %> <%= AppConfig.site.name %>.</p>
      </div>
    </footer>
  </main>

  <!-- Menu -->
  <aside class="ls-sidebar">
    <nav class="ls-menu">
      <ul>
        <%= menu_tag "Posts",      admin_posts_path,       /post/,       class: 'ls-ico-home' %>
        <%= menu_tag "Categories", admin_categories_path,  /categories/, class: 'ls-ico-flag' %>
        <%= menu_tag "Pages",      admin_pages_path,       /pages/,      class: 'ls-ico-text' %>
        <%= menu_tag "Contacts",   admin_contacts_path,    /contacts/,   class: 'ls-ico-envelope' %>
      </ul>
    </nav>
  </aside>

  <!-- Scripts -->
  <script type="text/javascript" src="//ajax.googleapis.com/ajax/libs/jquery/2.0.3/jquery.min.js"></script>
  <script src="<%= AppConfig.locastyle %>/javascripts/locastyle.js" type="text/javascript"></script>
  <%= javascript_include_tag :application %>
</body>
</html>
